snapshot.tar.xz and runtime-version.txt are generated by build-termux.sh.
Do not replace snapshot.tar.xz with an arbitrary Linux runtime: the APK needs the Android/Termux runtime and compatibility patches.
