name: Validate startup regex fix

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/validate-startup-regex.yml"

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
      - name: Static checks
        shell: bash
        run: |
          set -euo pipefail
          bash scripts/check-remaining-fixes.sh
      - name: Kotlin compile
        shell: bash
        run: |
          chmod +x gradlew
          ./gradlew :app:compileDebugKotlin --no-daemon
